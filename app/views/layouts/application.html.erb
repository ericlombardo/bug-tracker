<!DOCTYPE html>
<html>
  <head>
    <title>BugTracker</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <br><br>
    <!-- this should render in a partial-->
    <% if flash.any? %> <!-- checks for any flash messages -->
      <% flash.each do |type, mes| %> <!-- iterates through each one present-->
        <div class="<%= type %>"><%= mes %></div> <!-- creates a div with type of alert and displays message-->
      <% end %>
    <% end %>
    <% if current_user %>
      <ul class="nav flex-column">
        <li class="nav-item">
          <%= link_to "Overview", user_path(current_user), class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Programs", programs_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "My Programs", user_programs_path(current_user), class: "nav-link" %>
        </li>
        <li class="nav-item">
          <a class="nav-link"href="#">My Bugs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link"href="#">Employees</a>
        </li>
        <li class="nav-item">
          <%= link_to "Logout", logout_path, method: "post", class: "nav-link"  %>
        </li>
      </ul>
    <% end %>
    <%= yield %>
  </body>
</html>
