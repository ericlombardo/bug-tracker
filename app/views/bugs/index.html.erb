<%= render partial: 'bug_title', locals: {title: choose_title(@program, @user)} %>

<% if @bugs %>
  <% @bugs.each do |bug| %>
    <%= bug.status %><br>
    <%= bug.priority %><br>
    <%= bug.overview %><br>
    <%= bug.category %><br><br>
  <% end %>
<% end %>

<%= link_to("Report Bug for #{@program.name}", new_program_bug_path(@program), class: "btn btn-sm btn-primary") if @program %>
