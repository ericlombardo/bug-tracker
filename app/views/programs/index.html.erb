Programs index
<ul>
  <% if @programs.present? %>
    <% @programs.each do |p| %>
      <li><%= link_to p.name, p %></li>
    <% end %>
  <% end %>
</ul>

<%= link_to("New Program", new_program_path, class: "btn btn-primary") if admin? %>

