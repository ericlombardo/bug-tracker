<% @tasks.each do |t| %>
  <%= link_to t.title, user_task_path(t.user, t) %><br>
  <%= t.urgency %><br>
<% end %>

<%= link_to "Make New Task", new_user_task_path(current_user), class: "btn btn-sm btn-primary" %>