<% tasks.each do |t| %>
<div class="card bg-secondary mt-3">
  <div class="card-text">
    <p>Urgency: <%= t.urgency.capitalize %></p>
    <%= link_to t.title, user_task_path(t.user, t) %> 
  </div>
</div>
<% end %>